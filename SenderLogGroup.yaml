Resources:
  MyLogGroup:
    Type: AWS::Logs::LogGroup
    Properties:
      LogGroupName: sender-log-group
      RetentionInDays: 7

Outputs:
  MyLogGroupArn:
    Value: !GetAtt MyLogGroup.Arn
    Export:
      Name: SenderLogGroupArn
  
  MyLogGroupName:
    Description: LogGroupの名前
    Value: source-log-group
    Export:
      Name: SenderLogGroupName
